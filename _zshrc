# File: .zshrc
#Author: TingoL
#Contact e-mail: amelxmx [at] gmail [dot] com

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi
zstyle ':completion:*' insert-tab false

alias zconfig="vim ~/.zshrc"

# Customize to your needs...
export PATH=$PATH:/home/tingol/Skripte:/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin:/usr/bin/core_perl
#

#file extension aliases

alias -s smc=zsnes
alias -s pdf=zathura
alias -s tex=vim
alias -s mp3=mplayer
alias -s mkv=mplayer
alias -s avi=mplayer
alias -s mp4=mplayer
alias -s jpg=sxiv
alias -s png=sxiv
alias -s py=vim
alias -s c=vim
alias -s html=dwb

. $HOME/.shell-alias
. $HOME/.shell-functions
# . /usr/share/zsh/site-contrib/powerline.zsh
eval "$(fasd --init auto)"
HIST_IGNORE_ALL_DUPS="true"
export HISTSIZE=3000
export SAVEHIST=3000
export KEYTIMEOUT=1
export BROWSER=/usr/bin/dwb
export EDITOR=/usr/bin/vim
export VISUAL=/usr/bin/vim
export LANG=en_US.UTF-8
export XDG_CONFIG_HOME=/home/tingol/.config
export WORKON_HOME=~/Envs
VIRTUALENVWRAPPER_PYTHON=/usr/bin/python2
. /usr/bin/virtualenvwrapper.sh
